set(TEST_LIBRARIES ${DCA_EXTERNAL_LIBS})
set(TEST_INCLUDES  ${CMAKE_CURRENT_SOURCE_DIR} ${DCA_INCLUDES})

add_gtest(NAME "posix_qmci_seed_test"
        MPI MPI_NUMPROC 1
        WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
        INCLUDES "${TEST_INCLUDES}"
        LIBS     "${TEST_LIBRARIES}"
        )
